# @package _global_

model:
  name: resnet50
  num_classes: 10
  pretrained: false
  image_size: 32

train:
  epochs: 50
  batch_size: 64
  learning_rate: 0.1
  weight_decay: 5e-4
  optimizer: sgd
  momentum: 0.9
  device: cuda